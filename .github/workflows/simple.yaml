name: Python Executable

on: 
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: docker build --file Dockerfile --tag hello-world:$(echo ${GITHUB_SHA} | cut -c1-7) .
    - name: Login to GitHub Container Registry
      uses: docker/login-action@v1
      with:
        registry: ghcr.io
        username: karthikinala
        password: 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
